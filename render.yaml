# Blueprint for this repo only (separate from other Render apps).
# Create via Dashboard: https://dashboard.render.com/blueprints → New Blueprint Instance
services:
  - type: web
    name: rhye-themeforest-wp
    runtime: docker
    plan: starter
    region: oregon
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /
    disk:
      name: rhye-tf-uploads
      mountPath: /var/www/html/wp-content/uploads
      sizeGB: 5
    envVars:
      - key: WORDPRESS_DB_HOST
        fromDatabase:
          name: rhye-themeforest-db
          property: host
      - key: WORDPRESS_DB_USER
        fromDatabase:
          name: rhye-themeforest-db
          property: user
      - key: WORDPRESS_DB_PASSWORD
        fromDatabase:
          name: rhye-themeforest-db
          property: password
      - key: WORDPRESS_DB_NAME
        fromDatabase:
          name: rhye-themeforest-db
          property: database
      - key: PORT
        value: "80"
      - key: AUTH_KEY
        generateValue: true
      - key: SECURE_AUTH_KEY
        generateValue: true
      - key: LOGGED_IN_KEY
        generateValue: true
      - key: NONCE_KEY
        generateValue: true
      - key: AUTH_SALT
        generateValue: true
      - key: SECURE_AUTH_SALT
        generateValue: true
      - key: LOGGED_IN_SALT
        generateValue: true
      - key: NONCE_SALT
        generateValue: true

databases:
  - name: rhye-themeforest-db
    plan: free
    databaseName: wordpress
